## interceptors

http interceptors to inject `x-auth-token` for requests and display alerts during 4xx and 500 response.

## Dependencies

    sc-alerts

## Usage

Just include `sc-interceptors` module as a dependency.

```js
angular.module('APP', [
  sc-interceptors
])
```
